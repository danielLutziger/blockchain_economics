/*import React from "react";

const optionTypes= {
    CALL: 'call',
    PUT: 'put',
}

class Option {

    constructor(props) {
        this.option = {
            type: props.type,
            strikePrice: props.strikePrice,
            premium: props.premium,
            expiration: props.expiration,
            tknAmnt: props.tknAmnt,
            exercised: props.exercised,
            canceled: props.canceled,
            id: props.id,
            costToExercise: props.costToExercise,
            seller: props.seller,
            buyer: props.buyer
        }
    }

} export default Option;*/